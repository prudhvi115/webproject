services:
  - type: web
    name: astra-learn
    env: python
    region: oregon
    plan: starter
    branch: main
    autoDeploy: true
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn study_platform.wsgi
